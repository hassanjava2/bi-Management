# BI Management - Root Environment Template
# Copy to .env and set your own values. NEVER commit .env
# Used by root docker-compose and scripts.

# Database (for docker-compose postgres service)
DB_USER=bi_admin
DB_PASSWORD=your-secure-db-password-change-me

# Redis (for docker-compose redis service)
REDIS_PASSWORD=your-secure-redis-password-change-me

# JWT - Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Encryption - 64 hex chars: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_KEY=your-32-byte-hex-encryption-key

NODE_ENV=development
