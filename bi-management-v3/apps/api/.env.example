# BI Management v3 API - Environment Template
# Copy to .env and set your own values. NEVER commit .env

# Database - Match credentials to your PostgreSQL / docker-compose
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5433/bi_management_v3

# JWT - Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-jwt-secret-change-in-production-min-32-chars
JWT_EXPIRES=7d

# CORS - Comma-separated allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
