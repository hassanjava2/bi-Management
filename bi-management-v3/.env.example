# BI Management v3 - Environment Template
# Copy to .env and set your own values. NEVER commit .env

# API
PORT=3001

# JWT - Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-jwt-secret-min-32-chars-change-in-production
JWT_EXPIRES=7d

# PostgreSQL - Use same user/password as in docker-compose when using Docker
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5433/bi_management_v3

# CORS - Allowed frontend origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
