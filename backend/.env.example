# BI Management API Configuration
# ====================================
# انسخ هذا الملف إلى .env وعدّل القيم
# IMPORTANT: Change all secrets in production!

# Server
PORT=3000
NODE_ENV=development   # development | production

# Database
DATABASE_PATH=../data/bi_management.db

# ====================================
# SECURITY - مهم جداً للإنتاج
# ====================================

# JWT Secret - مطلوب في الإنتاج (32+ حرف)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# Encryption Key - مطلوب في الإنتاج (64 hex chars = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ====================================
# Rate Limiting
# ====================================
RATE_LIMIT_WINDOW_MS=900000      # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100      # requests per window

# ====================================
# External Services
# ====================================
AI_ENGINE_URL=http://localhost:8000

# ====================================
# CORS - أصول مسموحة للـ Frontend
# ====================================
CORS_ORIGIN=http://localhost:5173

# ====================================
# Logging
# ====================================
LOG_LEVEL=debug   # debug | info | warn | error
