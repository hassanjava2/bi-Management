# 🚀 دليل تشغيل نظام BI للإدارة الذكية

## 📋 المتطلبات الأساسية

### 1. البرامج المطلوبة
- **Python 3.10+**: [تحميل من python.org](https://www.python.org/downloads/)
- **Node.js 18+**: [تحميل من nodejs.org](https://nodejs.org/)
- **Ollama** (اختياري للذكاء الاصطناعي): [تحميل من ollama.ai](https://ollama.ai/)

### 2. التحقق من التثبيت
```powershell
python --version    # يجب أن يكون 3.10 أو أحدث
node --version      # يجب أن يكون 18 أو أحدث
ollama --version    # اختياري
```

---

## 🏃 التشغيل السريع

### الطريقة الأولى: سكربت التشغيل التلقائي
```powershell
# من PowerShell
cd "d:\bi distor"
.\start.ps1
```

أو

```batch
# من Command Prompt
cd "d:\bi distor"
start.bat
```

### الطريقة الثانية: التشغيل اليدوي

#### الخطوة 1: تثبيت المتطلبات
```powershell
# متطلبات Python
cd "d:\bi distor\server"
pip install -r requirements.txt

# متطلبات الواجهة
cd "d:\bi distor\web"
npm install
```

#### الخطوة 2: تهيئة قاعدة البيانات
```powershell
cd "d:\bi distor\server"
python init_database.py
```

#### الخطوة 3: تشغيل السيرفر
```powershell
# نافذة 1 - API Server
cd "d:\bi distor\server"
python main.py
```

#### الخطوة 4: تشغيل الواجهة
```powershell
# نافذة 2 - Web Dashboard
cd "d:\bi distor\web"
npm run dev
```

---

## 🌐 الوصول للنظام

| الخدمة | الرابط |
|--------|--------|
| لوحة التحكم | http://localhost:3000 |
| API Server | http://localhost:8000 |
| توثيق API | http://localhost:8000/docs |

---

## 🤖 إعداد الذكاء الاصطناعي المحلي

### 1. تثبيت Ollama
- حمل من: https://ollama.ai/
- ثبت البرنامج
- أعد تشغيل الكمبيوتر

### 2. تحميل النموذج
```powershell
# نموذج مقترح (خفيف وسريع)
ollama pull llama3.2

# أو نموذج أقوى
ollama pull qwen2.5:7b
```

### 3. التحقق من العمل
```powershell
ollama list  # يعرض النماذج المثبتة
ollama run llama3.2 "مرحبا"  # اختبار
```

---

## 📁 هيكل المشروع

```
d:\bi distor\
├── data\                    # بيانات الشركة
│   ├── employees.json       # بيانات الموظفين
│   ├── company.json         # معلومات الشركة
│   └── departments.json     # الأقسام
├── server\                  # السيرفر (Python FastAPI)
│   ├── main.py             # API الرئيسي
│   ├── services\           # الخدمات
│   │   └── ai_service.py   # خدمة AI
│   └── init_database.py    # تهيئة قاعدة البيانات
├── web\                     # الواجهة (Next.js)
│   ├── src\
│   │   ├── app\            # الصفحات
│   │   └── components\     # المكونات
│   └── package.json
├── system\                  # ملفات النظام
│   ├── database\           # قاعدة البيانات
│   └── ai\                 # بيانات تدريب AI
├── start.bat               # سكربت تشغيل Windows
└── start.ps1               # سكربت تشغيل PowerShell
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة: "python غير معروف"
**الحل**: تأكد من إضافة Python لـ PATH أثناء التثبيت

### المشكلة: "npm غير معروف"
**الحل**: أعد تثبيت Node.js وتأكد من تفعيل "Add to PATH"

### المشكلة: السيرفر لا يعمل
**الحل**: 
```powershell
cd "d:\bi distor\server"
pip install -r requirements.txt --force-reinstall
```

### المشكلة: الواجهة لا تعمل
**الحل**:
```powershell
cd "d:\bi distor\web"
rm -r node_modules
npm install
```

### المشكلة: AI لا يرد
**الحل**: تأكد من تشغيل Ollama:
```powershell
ollama serve
```

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
- **المالك**: حسن فؤاد - 07715473797
- **GitHub**: [BI Systems]

---

## 🔮 التحديثات المستقبلية

- [ ] تطبيق موبايل للموظفين
- [ ] نظام تحليل كاميرات المراقبة
- [ ] ربط مع شركات التوصيل
- [ ] تقارير متقدمة بالذكاء الاصطناعي

---

**BI للحاسبات - Beyond Intelligence** 🚀
